---
import '../styles/global.css';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
import Card from '../components/Card.astro';

// @ts-ignore
import { env } from 'cloudflare:workers';

type EventItem = {
  date: string;
  title: string;
  description: string;
  join_url?: string | null;
};

type ProgramItem = {
  date: string;
  title: string;
  location?: string | null;
  notes?: string | null;
  url?: string | null;
  speaker?: string | null;
  is_featured?: number | null;
};

type ExpertItem = {
  name: string;
  role: string;
  linkedin_url?: string | null;
  is_founder?: number | null;
};

const db = (env as any)?.mief_db;
let allEvents: EventItem[] = [];
let dbError = null;

if (db) {
  try {
    const result = await db.prepare("SELECT * FROM events").all();
    allEvents = (result.results || []) as EventItem[];
  } catch (e) {
    console.error("Database query error:", e);
    dbError = "Nem sikerült betölteni az eseményeket.";
  }
}

// Programajánló lekérése
let recommendedPrograms: ProgramItem[] = [];
if (db) {
  try {
    const result = await db.prepare("SELECT * FROM programs ORDER BY date ASC").all();
    recommendedPrograms = (result.results || []) as ProgramItem[];
  } catch (e) {
    console.error("Programs query error:", e);
  }
}

// Szakértők lekérése
let experts: ExpertItem[] = [];
if (db) {
  try {
    const result = await db.prepare("SELECT * FROM experts ORDER BY is_founder DESC, created_at ASC").all();
    experts = (result.results || []) as ExpertItem[];
  } catch (e) {
    console.error("Experts query error:", e);
  }
}

const upcomingEvents = allEvents
  .sort((a, b) => new Date(b.date).getTime() - new Date(a.date).getTime());

let submissionStatus = null;

if (Astro.request.method === "POST") {
  try {
    const data = await Astro.request.formData();
    const name = data.get("name");
    const email = data.get("email");
    const professionSelect = data.get("profession");
    const professionCustom = data.get("profession_custom");
    
    const finalProfession = professionSelect === "other" ? professionCustom : professionSelect;

    if (db && name && email && finalProfession) {
      await db.prepare("INSERT INTO registrations (name, email, profession) VALUES (?, ?, ?)")
        .bind(name, email, String(finalProfession))
        .run();
      submissionStatus = "success";
    } else {
      submissionStatus = "error";
    }
  } catch (error) {
    console.error("Registration error:", error);
    submissionStatus = "error";
  }
}
---

<html lang="hu" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" href="/favicon.ico" />
    <title>MI Etika Fórum | MIEF</title>
    <meta
      name="description"
      content="Az MI Etika Fórum szakmai közösség a mesterséges intelligencia felelős, átlátható és emberközpontú alkalmazásáról."
    />
  </head>

  <body class="min-h-screen bg-slate-900 text-slate-100 antialiased selection:bg-cyan-500/30">
    <Navbar />

    <main>
      {/* Hero Section */}
      <section class="relative min-h-[80vh] flex items-center overflow-hidden border-b border-white/5">
        <div class="absolute inset-0 bg-[radial-gradient(circle_at_50%_50%,rgba(34,211,238,0.05),transparent_50%)]"></div>
        <div class="absolute top-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-cyan-500/50 to-transparent"></div>

        <div class="container mx-auto px-6 relative z-10 py-24 sm:py-32">
          <div class="grid gap-16 lg:grid-cols-2 lg:items-center">
            <div>
              <div class="inline-flex items-center gap-2 rounded-full border border-cyan-400/20 bg-cyan-400/10 px-4 py-1.5 text-sm font-medium text-cyan-300">
                <span class="relative flex h-2 w-2">
                  <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-cyan-400 opacity-75"></span>
                  <span class="relative inline-flex rounded-full h-2 w-2 bg-cyan-500"></span>
                </span>
                Szakmai közösség
              </div>

              <h1 class="mt-8 text-5xl font-extrabold tracking-tight text-white sm:text-7xl">
                MIEF – ahol az MI technológiai kérdésből <br />
                <span class="bg-gradient-to-r from-cyan-400 to-blue-500 bg-clip-text text-transparent">társadalmi felelősséggé válik</span>
              </h1>

              <p class="mt-8 max-w-2xl text-lg leading-relaxed text-slate-300 sm:text-xl">
                A MIEF független, interdiszciplináris szakmai műhely, amely jogi, technológiai, oktatási, filozófiai, üzleti és biztonságpolitikai nézőpontból vizsgálja a mesterséges intelligencia hatásait. Célunk, hogy a felelős MI-használatról ne csak beszéljünk, hanem alkalmazható tudást, szakmai ajánlásokat és élő szakértői hálózatot építsünk.
              </p>

              <div class="mt-9 sm:mt-10 flex flex-col gap-3 sm:gap-4 sm:flex-row">
                <a
                  href="#regisztracio"
                  class="rounded-xl bg-cyan-400 px-8 py-4 text-center text-sm font-bold text-slate-900 shadow-xl shadow-cyan-500/20 transition hover:bg-cyan-300 hover:scale-105 active:scale-95"
                >
                  Csatlakozás a műhelyhez
                </a>
                <a
                  href="#esemenyek"
                  class="rounded-xl border border-white/10 bg-white/5 px-8 py-4 text-center text-sm font-bold text-white backdrop-blur-sm transition hover:bg-white/10"
                >
                  Események megtekintése
                </a>
              </div>
            </div>

            {/* Image Illustration */}
            <div class="relative hidden lg:block">
              <div class="absolute inset-0 bg-gradient-to-br from-cyan-500/20 to-blue-500/20 blur-3xl"></div>
              <div class="relative rounded-3xl border border-white/10 bg-slate-800/50 p-4 backdrop-blur-2xl overflow-hidden">
                 <img src="/assets/logo.jpg" alt="MIEF Illustration" class="w-full h-auto rounded-2xl shadow-2xl opacity-90 transition hover:scale-105 duration-700" />
              </div>
              <div class="absolute -bottom-6 -right-6 h-32 w-32 rounded-full border border-cyan-400/20 bg-cyan-400/10 blur-xl"></div>
              <div class="absolute -top-6 -left-6 h-32 w-32 rounded-full border border-blue-400/20 bg-blue-400/10 blur-xl"></div>
            </div>
          </div>
        </div>
      </section>

      {/* Bizalom / Hitelesség Section */}
      <section class="py-10 sm:py-12 border-y border-white/5 bg-slate-950/80">
        <div class="container mx-auto px-6">
          <div class="grid gap-4 md:grid-cols-3">
            <div class="rounded-2xl border border-white/10 bg-slate-900/70 px-5 py-4">
              <p class="micro-label text-cyan-300">Interdiszciplináris műhely</p>
              <p class="mt-2 text-sm text-slate-300 leading-relaxed">Jogi, technológiai, oktatási és biztonságpolitikai nézőpontok egy közös szakmai térben.</p>
            </div>
            <div class="rounded-2xl border border-white/10 bg-slate-900/70 px-5 py-4">
              <p class="micro-label text-cyan-300">Független fórum</p>
              <p class="mt-2 text-sm text-slate-300 leading-relaxed">Szakmai érvek mentén működő közösség, pártpolitikai és szűk üzleti kötöttség nélkül.</p>
            </div>
            <div class="rounded-2xl border border-white/10 bg-slate-900/70 px-5 py-4">
              <p class="micro-label text-cyan-300">Nyitott csatlakozás</p>
              <p class="mt-2 text-sm text-slate-300 leading-relaxed">Szakértőknek, kutatóknak és gyakorlati döntéshozóknak, akik felelős MI-használatban gondolkodnak.</p>
            </div>
          </div>
        </div>
      </section>

      {/* Küldetés Section */}
      <section id="miert" class="py-20 sm:py-24 relative overflow-hidden bg-slate-900">
        <div class="container mx-auto px-6">
          <div class="max-w-4xl mx-auto text-center">
            <h2 class="section-kicker">Kik vagyunk?</h2>
            <p class="mt-10 section-copy">
              A <strong>MIEF – MI Etika Fórum</strong> – olyan független szakmai közösség és műhely, amely a mesterséges intelligencia társadalmi, jogi, etikai és biztonsági következményeit vizsgálja. Közösségünkben különböző területek szakemberei dolgoznak együtt: jogászok, informatikusok, pedagógusok, filozófusok, kutatók, üzleti szereplők, valamint katonai és biztonságpolitikai szakértők.
            </p>
            <p class="mt-6 section-copy">
              Hiszünk abban, hogy a mesterséges intelligencia nem pusztán mérnöki vagy üzleti kérdés. Az MI-rendszerek döntéseket, intézményeket, oktatást, munkát, biztonságot és társadalmi bizalmat alakítanak. Ezért olyan térre van szükség, ahol a technológiai innováció mellett az emberi méltóság, a felelősség, az átláthatóság és a közjó szempontjai is megjelennek.
            </p>
            <p class="mt-6 section-copy">
              A MIEF célja, hogy szakmai párbeszédből alkalmazható tudás szülessen: ajánlások, háttéranyagok, képzések, műhelymunkák és közérthető szakmai tartalmak formájában.
            </p>
          </div>
        </div>
      </section>

      {/* Miért fontos az MI-etika? Section */}
      <section id="kinek" class="py-20 sm:py-24 border-y border-white/5 bg-slate-950/70">
        <div class="container mx-auto px-6">
          <div class="mb-14 sm:mb-16 text-center max-w-3xl mx-auto">
            <h2 class="section-kicker">Miért fontos az MI-etika?</h2>
            <h3 class="section-heading">Interdiszciplináris válaszok a jövő kihívásaira</h3>
            <p class="section-intro">
              Az MI-etika nem oldható meg egyetlen szakterület logikájával. A MIEF értéke abban áll, hogy különböző szakmai nézőpontokat kapcsol össze, és ezekből közérthető, gyakorlati és felelősségteljes válaszokat keres.
            </p>
          </div>
          
          <div class="grid gap-8 md:grid-cols-3 max-w-6xl mx-auto">
            <div class="rounded-2xl border border-white/10 bg-slate-900/60 p-8 transition hover:border-cyan-400/50">
              <h4 class="text-xl font-bold text-white mb-4">Valódi interdiszciplinaritás</h4>
              <p class="text-slate-300 text-sm leading-relaxed">
                Nálunk a jogász, a mérnök, a pedagógus, a filozófus, a teológus, az üzleti szakember és a biztonságpolitikai gondolkodó valóban egy asztalnál ül. Nem csak egy részproblémát vizsgálunk, hanem az MI társadalmi hatásainak teljesebb képét.
              </p>
            </div>
            <div class="rounded-2xl border border-white/10 bg-slate-900/60 p-8 transition hover:border-cyan-400/50">
              <h4 class="text-xl font-bold text-white mb-4">Független szakmai működés</h4>
              <p class="text-slate-300 text-sm leading-relaxed">
                Civil szakmai fórumként arra törekszünk, hogy elemzéseinket és ajánlásainkat ne pártpolitikai vagy szűk üzleti érdekek, hanem szakmai érvek, közérdekű szempontok, emberi méltóság és társadalmi felelősség vezesse.
              </p>
            </div>
            <div class="rounded-2xl border border-white/10 bg-slate-900/60 p-8 transition hover:border-cyan-400/50">
              <h4 class="text-xl font-bold text-white mb-4">Gyakorlati fókusz</h4>
              <p class="text-slate-300 text-sm leading-relaxed">
                Nem pusztán elméleti vitákat folytatunk. Célunk olyan kézzelfogható outputok létrehozása, mint rövid szakmai háttéranyagok, képzési anyagok, esettanulmányok, ellenőrzőlisták és ajánlások.
              </p>
            </div>
          </div>

          <div class="mt-16 flex flex-wrap justify-center gap-4 max-w-5xl mx-auto">
            {['Jogászok', 'Pedagógusok', 'IT-szakemberek', 'Filozófusok', 'Üzletemberek', 'Egyetemi oktatók', 'Kutatók', 'Katonai és biztonságpolitikai szakértők', '...és további szakterületek képviselői'].map((target) => (
              <span class="rounded-full border border-white/10 bg-white/5 px-6 py-2 text-sm font-medium text-slate-200 transition hover:border-cyan-400/50 hover:bg-cyan-400/10 hover:text-cyan-300">
                {target}
              </span>
            ))}
          </div>
        </div>
      </section>

      {/* LinkedIn / News Section */}
      <section id="hirek" class="py-20 sm:py-24 relative overflow-hidden bg-slate-900">
        <div class="absolute inset-0 bg-cyan-500/5 [mask-image:radial-gradient(ellipse_at_center,transparent_20%,black)]"></div>
        <div class="container mx-auto px-6 relative z-10">
          <div class="relative rounded-3xl border border-white/10 bg-slate-900/80 p-7 sm:p-8 md:p-12 overflow-hidden group">
            <div class="absolute top-0 right-0 -mt-20 -mr-20 h-64 w-64 rounded-full bg-cyan-500/10 blur-3xl transition group-hover:bg-cyan-500/20"></div>
            
            <div class="grid gap-12 lg:grid-cols-2 lg:items-center">
              <div>
                <h2 class="section-kicker">Tudástár</h2>
                <h3 class="section-heading">Szakmai anyagok és állásfoglalások</h3>
                <p class="section-copy">
                  A MIEF Tudástár célja, hogy közérthető, szakmailag megalapozott és gyakorlati szempontból is használható anyagokat tegyen közzé a mesterséges intelligencia etikai, jogi, oktatási, társadalmi és biztonsági kérdéseiről.
                </p>
                <div class="mt-8 p-6 rounded-2xl bg-cyan-400/5 border border-cyan-400/20">
                  <p class="text-cyan-300 font-bold mb-2 flex items-center gap-2">
                    <span class="animate-pulse h-2 w-2 rounded-full bg-cyan-400"></span>
                    Fejlesztés alatt
                  </p>
                  <p class="text-sm text-slate-300 leading-relaxed">
                    A Tudástár fejlesztés alatt áll. A következő időszakban rövid szakmai háttéranyagokkal (MIEF Briefs), ajánlásokkal, esettanulmányokkal és állásfoglalásokkal bővítjük felületünket.
                  </p>
                </div>
                <div class="mt-8">
                  <p class="text-sm font-bold text-white mb-4 uppercase tracking-widest">Tervezett témáink:</p>
                  <div class="flex flex-wrap gap-2">
                    {['AI Act és felelősség', 'MI az oktatásban', 'Deepfake és bizalom', 'Kiberbiztonság', 'MI a közszolgálatban', 'Katonai etika', 'Etikai ellenőrzőlisták'].map((topic) => (
                      <span class="micro-label bg-slate-800 text-slate-300 px-3 py-1 rounded border border-white/5">
                        {topic}
                      </span>
                    ))}
                  </div>
                </div>
              </div>
              
              <div class="relative">
                <div class="grid gap-4 md:grid-cols-2 md:auto-rows-fr">
                  <div class="rounded-2xl border border-cyan-400/20 bg-slate-950/90 p-6 md:col-span-2 md:p-8 transform hover:-translate-y-1 transition duration-500">
                    <div class="flex items-center gap-3 mb-4">
                      <div class="h-10 w-10 rounded bg-cyan-500/20 flex items-center justify-center">
                        <svg class="h-5 w-5 text-cyan-300" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M8 3h6l4 4v14H8z" />
                          <path stroke-linecap="round" stroke-linejoin="round" d="M14 3v5h4" />
                          <path stroke-linecap="round" stroke-linejoin="round" d="M10 12h6M10 16h6" />
                        </svg>
                      </div>
                      <span class="micro-label text-cyan-400">MIEF Briefs</span>
                    </div>
                    <p class="text-sm md:text-base text-slate-300 italic">"Rövid, 2–4 oldalas szakmai háttéranyagok aktuális MI-etikai kérdésekről..."</p>
                  </div>
                  <div class="rounded-2xl border border-white/10 bg-slate-950/80 p-6 md:translate-y-3 transform hover:-translate-y-1 transition duration-500 hidden sm:block">
                    <div class="flex items-center gap-3 mb-4">
                      <div class="h-10 w-10 rounded bg-blue-500/20 flex items-center justify-center">
                        <svg class="h-5 w-5 text-blue-300" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M4 12v-2a1 1 0 011-1h2l8-4v14l-8-4H5a1 1 0 01-1-1v-2z"/>
                          <path stroke-linecap="round" stroke-linejoin="round" d="M18 10.5a3.5 3.5 0 010 3"/>
                        </svg>
                      </div>
                      <span class="micro-label text-blue-400">Állásfoglalások</span>
                    </div>
                    <p class="text-sm text-slate-300 italic">"A szakértői közösség által elfogadott szakmai vélemények..."</p>
                  </div>
                  <div class="rounded-2xl border border-white/10 bg-slate-950/80 p-6 md:-translate-y-3 transform hover:-translate-y-1 transition duration-500">
                    <div class="flex items-center gap-3 mb-4">
                      <div class="h-10 w-10 rounded bg-indigo-500/20 flex items-center justify-center">
                        <svg class="h-5 w-5 text-indigo-300" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M9 14a5 5 0 116 0c-.7.7-1.2 1.7-1.3 2.7H10.3c-.1-1-.6-2-1.3-2.7z"/>
                          <path stroke-linecap="round" stroke-linejoin="round" d="M10 20h4"/>
                        </svg>
                      </div>
                      <span class="micro-label text-indigo-400">Ajánlások</span>
                    </div>
                    <p class="text-sm text-slate-300 italic">"Gyakorlati útmutatók döntéshozók és szakemberek számára..."</p>
                  </div>
                </div>
                
                <div class="mt-10 sm:mt-12 text-center lg:text-left">
                  <p class="text-sm text-slate-300 mb-4">Aktuális híreinkért és rövidebb bejegyzéseinkért kövess minket LinkedIn-en:</p>
                  <a 
                    href="https://www.linkedin.com/company/113130027" 
                    target="_blank"
                    class="inline-flex items-center gap-3 rounded-xl bg-white/5 border border-white/10 px-6 py-3 text-sm font-bold text-white transition hover:bg-white/10 hover:border-cyan-400/50 hover:scale-[1.02]"
                  >
                    <svg class="h-6 w-6 text-cyan-400" fill="currentColor" viewBox="0 0 24 24"><path d="M19 0h-14c-2.761 0-5 2.239-5 5v14c0 2.761 2.239 5 5 5h14c2.762 0 5-2.239 5-5v-14c0-2.761-2.238-5-5-5zm-11 19h-3v-11h3v11zm-1.5-12.268c-.966 0-1.75-.79-1.75-1.764s.784-1.764 1.75-1.764 1.75.79 1.75 1.764-.783 1.764-1.75 1.764zm13.5 12.268h-3v-5.604c0-3.368-4-3.113-4 0v5.604h-3v-11h3v1.765c1.396-2.586 7-2.777 7 2.476v6.759z"/></svg>
                    MIEF LinkedIn oldal
                  </a>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* Members & Experts Section */}
      <section id="tagok" class="py-20 sm:py-24 relative overflow-hidden bg-slate-950/60 border-y border-white/5">
        <div class="container mx-auto px-6">
          <div class="mb-14 sm:mb-16 text-center">
            <h2 class="section-kicker">Szakértőink</h2>
            <h3 class="section-heading">Akik a párbeszédet építik</h3>
            <p class="section-intro max-w-2xl mx-auto">
              A MIEF szakértői különböző területekről érkeznek, hogy az MI hatásait jogi, etikai, társadalmi és biztonsági kihívásként vizsgálják.
            </p>
          </div>

          <div class="max-w-5xl mx-auto">
            {/* Founder */}
            {(() => {
              const founder = experts.find(e => e.is_founder);
              const regularExperts = experts.filter(e => !e.is_founder);
              return (
                <>
                  {founder && (
                    <div class="mb-16 rounded-3xl border border-cyan-400/20 bg-cyan-400/5 p-8 flex flex-col md:flex-row md:items-center justify-between gap-6 shadow-lg shadow-cyan-500/5">
                      <div>
                        <span class="micro-label text-cyan-400 mb-2 block">Alapító</span>
                        <h4 class="text-2xl font-bold text-white italic">{founder.name}</h4>
                        <p class="mt-2 text-slate-300 leading-relaxed">{founder.role}</p>
                      </div>
                      {founder.linkedin_url && (
                        <a href={founder.linkedin_url} target="_blank" class="inline-flex items-center gap-2 text-cyan-400 hover:text-cyan-300 transition font-medium">
                          <svg class="h-6 w-6" fill="currentColor" viewBox="0 0 24 24"><path d="M19 0h-14c-2.761 0-5 2.239-5 5v14c0 2.761 2.239 5 5 5h14c2.762 0 5-2.239 5-5v-14c0-2.761-2.238-5-5-5zm-11 19h-3v-11h3v11zm-1.5-12.268c-.966 0-1.75-.79-1.75-1.764s.784-1.764 1.75-1.764 1.75.79 1.75 1.764-.783 1.764-1.75 1.764zm13.5 12.268h-3v-5.604c0-3.368-4-3.113-4 0v5.604h-3v-11h3v1.765c1.396-2.586 7-2.777 7 2.476v6.759z"/></svg>
                          LinkedIn profil
                        </a>
                      )}
                    </div>
                  )}
                  <div class="grid gap-4 md:grid-cols-2 lg:grid-cols-3">
                    {regularExperts.slice(0, 5).map((expert) => (
                      <div class="rounded-2xl border border-white/5 bg-white/[0.02] p-6 hover:border-white/10 transition group">
                        <h5 class="font-bold text-white group-hover:text-cyan-400 transition">{expert.name}</h5>
                        <p class="mt-2 text-sm text-slate-300 leading-normal">{expert.role}</p>
                      </div>
                    ))}
                    <a href="/szakertok" class="rounded-2xl border border-dashed border-cyan-400/20 bg-cyan-400/5 p-6 flex items-center justify-center group transition hover:bg-cyan-400/10">
                      <span class="text-cyan-400 font-bold text-sm group-hover:scale-105 transition">Összes szakértő megtekintése →</span>
                    </a>
                  </div>
                </>
              );
            })()}
          </div>
        </div>
      </section>

      {/* Mivel foglalkozunk? Section */}
      <section id="tevekenyseg" class="py-20 sm:py-24 bg-slate-900">
        <div class="container mx-auto px-6">
          <div class="mb-14 sm:mb-16 text-center max-w-3xl mx-auto">
            <h2 class="section-kicker">Mivel foglalkozunk?</h2>
            <h3 class="section-heading">Szakmai pilléreink</h3>
            <p class="section-intro">
              A MIEF nem egyetlen szakma válaszát keresi az MI kihívásaira. Működésünk célja, hogy a kutatás, a szakmai párbeszéd, az oktatás, a szabályozási tájékozódás és a közösségépítés összekapcsolódjon.
            </p>
          </div>

          <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3 max-w-6xl mx-auto">
            {[
              { title: 'Szakmai párbeszéd', desc: 'Online és személyes műhelymunka-alkalmakat, kerekasztal-beszélgetéseket és szakmai eseményeket szervezünk.' },
              { title: 'Kutatás és elemzés', desc: 'Szakmai háttéranyagokat és policy brief jellegű összefoglalókat készítünk az MI aktuális kérdéseiről.' },
              { title: 'Oktatás és képzés', desc: 'Workshopokat és szemléletformáló programokat dolgozunk ki döntéshozók és szakemberek számára.' },
              { title: 'Szabályozási támogatás', desc: 'Segítjük az AI Act, az ISO 42001 és más keretrendszerek etikai értelmezését.' },
              { title: 'Publikáció', desc: 'Független szakmai reflexiókat, esettanulmányokat és ajánlásokat teszünk közzé.' },
              { title: 'Közösségépítés', desc: 'Hazai szakértői hálózatot építünk, amely összeköti a különböző tudományterületeket.' }
            ].map((item) => (
              <div class="rounded-2xl border border-white/10 bg-slate-900/70 p-6 hover:border-cyan-400/30 transition">
                <h4 class="font-bold text-white mb-2">{item.title}</h4>
                <p class="text-sm text-slate-300 leading-relaxed">{item.desc}</p>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Fókuszterületek Section */}
      <section id="fokusz" class="py-20 sm:py-24 bg-slate-950/70 border-y border-white/5">
        <div class="container mx-auto px-6">
          <div class="mb-14 sm:mb-16 text-center max-w-3xl mx-auto">
            <h2 class="section-kicker">Szakértői területeink</h2>
            <h3 class="section-heading">Fókuszterületeink</h3>
            <p class="section-intro">
              A MIEF olyan területeken dolgozik, ahol a mesterséges intelligencia technológiai fejlődése közvetlenül érinti az emberi döntést, a társadalmi bizalmat, a jogi felelősséget, az oktatást és a biztonságot.
            </p>
          </div>

          <div class="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
            <Card 
              title="Emberközpontú és felelős MI" 
              description="Az emberi autonómia, méltóság és elszámoltathatóság kérdései az MI-rendszerek fejlesztésében."
              icon="shield"
            />
            <Card 
              title="MI-szabályozás és governance" 
              description="Az AI Act, az ISO 42001 és a hazai szabályozási környezet etikai és társadalmi értelmezése."
              icon="scales"
            />
            <Card 
              title="Oktatás és jövő" 
              description="Az MI hatása a tanulásra, a szakmai kompetenciákra és a felelős technológiahasználatra."
              icon="graduation"
            />
            <Card 
              title="Kiberbiztonság és reziliencia" 
              description="Az MI szerepe a digitális sérülékenységek és szervezeti kockázatok kezelésében."
              icon="building"
            />
            <Card 
              title="Közszolgálat és irányítás" 
              description="Az algoritmikus döntéstámogatás jogi és etikai következményei az állami működésben."
              icon="government"
            />
            <Card 
              title="Média és dezinformáció" 
              description="A deepfake, az álhírek és az algoritmikus tartalomterjesztés hatása a társadalmi bizalomra."
              icon="megaphone"
            />
            <Card 
              title="Védelem és biztonságpolitika" 
              description="Az MI katonai és stratégiai alkalmazásának etikai korlátai és döntéstámogatási dilemmái."
              icon="sword"
            />
          </div>

          <div class="mt-20 max-w-3xl mx-auto text-center border-t border-white/5 pt-12">
             <h3 class="text-2xl font-bold text-white mb-6">Miért csatlakozz hozzánk?</h3>
             <p class="text-slate-300 leading-relaxed mb-8">
               Célunk az alapvető értékek lefektetése egy olyan világban, ahol az algoritmusok mindennapjaink részévé válnak. Az MI Etika Fórum nem csupán elméleti vitafórum, hanem egy aktív hálózat, ahol a nézőpontok ütköztetése révén valódi, alkalmazható tudás jön létre.
             </p>
             <p class="text-slate-300 italic">
               „A szakember fogalma nálunk szándékosan sokrétű – hiszen a jövőt csak közösen érthetjük meg.”
             </p>
          </div>
        </div>
      </section>

      {/* Események Section */}
      <section id="esemenyek" class="py-20 sm:py-24 bg-slate-900">
        <div class="container mx-auto px-6">
          <div class="mb-14 sm:mb-16 flex flex-col md:flex-row md:items-end justify-between gap-5 sm:gap-6">
            <div>
              <h2 class="section-kicker">Műhelyek</h2>
              <h3 class="section-heading">Szakmai műhelymunkáink</h3>
              <p class="mt-4 text-slate-300 max-w-2xl leading-relaxed">
                Online és személyes műhelymunkáinkon a mesterséges intelligencia legégetőbb etikai és szabályozási kérdéseit dolgozzuk fel.
              </p>
            </div>
            <a href="/esemenyek" class="inline-flex items-center gap-2 rounded-xl bg-white/5 border border-white/10 px-6 py-3 text-sm font-bold text-white transition hover:bg-white/10 hover:border-cyan-400/50">
              Teljes műhelytár megnyitása →
            </a>
          </div>

          <div class="grid gap-8 lg:grid-cols-2">
            {upcomingEvents.length > 0 ? (
              upcomingEvents.slice(0, 4).map((event, index) => (
                <div class={`group relative rounded-3xl border p-6 sm:p-8 transition ${index === 0 ? 'lg:col-span-2 border-cyan-400/30 bg-slate-950' : 'border-white/10 bg-slate-950/70 hover:bg-slate-950'}`}>
                  <div class="flex flex-col md:flex-row md:items-center justify-between gap-4">
                    <div class="flex-1">
                      <div class="flex flex-wrap items-center gap-3 mb-2">
                        <span class="text-xs font-bold uppercase tracking-widest text-cyan-400 bg-cyan-400/10 px-2 py-1 rounded">
                          {new Date(event.date).toLocaleDateString('hu-HU', { year: 'numeric', month: 'long', day: 'numeric' })}
                        </span>
                        {new Date(event.date) < new Date() ? (
                          <span class="micro-label bg-slate-700 text-slate-300 px-2 py-0.5 rounded-full border border-white/5">
                            Lezajlott
                          </span>
                        ) : (
                          <span class="micro-label bg-cyan-500/20 text-cyan-300 px-2 py-0.5 rounded-full border border-cyan-400/20 animate-pulse">
                            Tervezett
                          </span>
                        )}
                      </div>
                      <h4 class={`mt-2 font-bold text-white group-hover:text-cyan-300 transition ${index === 0 ? 'text-xl sm:text-3xl leading-tight' : 'text-lg sm:text-xl leading-snug'}`}>{event.title}</h4>
                    </div>
                    {event.join_url && (
                      <a href={event.join_url} target="_blank" class={`inline-flex items-center justify-center rounded-xl px-6 py-3 text-sm font-bold transition ${index === 0 ? 'bg-cyan-400 text-slate-900 hover:bg-cyan-300' : 'bg-white/10 text-white hover:bg-white/20'}`}>
                        Csatlakozás
                      </a>
                    )}
                  </div>
                  <p class={`mt-5 sm:mt-6 text-slate-300 leading-relaxed whitespace-pre-wrap ${index === 0 ? 'line-clamp-3 sm:line-clamp-4 text-sm sm:text-base' : 'line-clamp-3 text-sm sm:text-base'}`}>
                    {event.description}
                  </p>
                </div>
              ))
            ) : (
              <p class="text-slate-300 italic text-lg">Jelenleg nincsenek meghirdetett események. Iratkozz fel, hogy értesülj a következőről!</p>
            )}
          </div>
        </div>
      </section>

      {/* Programajánló Section */}
      <section id="programok" class="py-20 sm:py-24 relative overflow-hidden bg-slate-950/60 border-y border-white/5">
        <div class="absolute inset-0 bg-emerald-500/5 [mask-image:radial-gradient(ellipse_at_center,white,transparent)]"></div>
        <div class="container mx-auto px-6 relative">
          <div class="mb-14 sm:mb-16">
            <h2 class="section-kicker-emerald">Programajánló</h2>
            <h3 class="section-heading">Szakmai események, konferenciák, fesztiválok</h3>
            <p class="mt-4 text-slate-300 max-w-2xl leading-relaxed">Válogatott külsős események, amelyeket ajánlunk az érdeklődőknek.</p>
          </div>

          <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
            {recommendedPrograms.length > 0 ? (
              recommendedPrograms.map((program) => (
                <div class={`group border rounded-3xl p-5 sm:p-6 transition flex flex-col h-full ${program.is_featured ? 'md:col-span-2 lg:col-span-2 border-emerald-400/30 bg-slate-950' : 'bg-slate-950/75 border-white/10 hover:border-emerald-400/30 hover:bg-slate-950'}`}>
                  <div class="flex-1">
                    <div class="flex items-center gap-2 mb-4">
                      <span class="text-xs font-bold text-emerald-400 bg-emerald-400/10 px-3 py-1 rounded-full border border-emerald-400/20">
                        {program.date}
                      </span>
                      {program.is_featured ? (
                        <span class="micro-label text-emerald-300 bg-emerald-500/20 px-2 py-1 rounded-full border border-emerald-400/30">Kiemelt</span>
                      ) : null}
                    </div>
                    <h4 class={`font-bold text-white group-hover:text-emerald-300 transition ${program.is_featured ? 'text-xl sm:text-3xl leading-tight line-clamp-3' : 'text-lg sm:text-xl leading-snug line-clamp-2'}`}>{program.title}</h4>
                    {program.location && (
                      <p class="mt-3 text-sm text-slate-300 flex items-center gap-2">
                        <svg class="h-4 w-4 text-emerald-300" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M12 21s7-5.5 7-11a7 7 0 10-14 0c0 5.5 7 11 7 11z" />
                          <circle cx="12" cy="10" r="2.5" />
                        </svg>
                        {program.location}
                      </p>
                    )}
                    {program.notes && (
                      <p class={`mt-4 text-slate-300 leading-relaxed italic ${program.is_featured ? 'text-sm sm:text-base line-clamp-3 sm:line-clamp-4' : 'text-sm line-clamp-3'}`}>
                        "{program.notes}"
                      </p>
                    )}
                    {program.speaker && (
                      <p class="mt-3 text-sm text-cyan-300 flex items-center gap-1.5">
                        <span class="text-base">🎤</span>
                        <span>Előadó: <strong>{program.speaker}</strong></span>
                      </p>
                    )}
                  </div>
                  
                  {program.url && (
                    <a href={program.url} target="_blank" class={`mt-8 inline-flex items-center justify-center rounded-xl border py-3 text-sm font-bold transition ${program.is_featured ? 'bg-emerald-500 text-slate-900 border-emerald-400 hover:bg-emerald-400' : 'bg-emerald-500/10 border-emerald-500/20 text-emerald-300 hover:bg-emerald-500 hover:text-slate-900'}`}>
                      Részletek megtekintése
                    </a>
                  )}
                </div>
              ))
            ) : (
              <p class="col-span-full text-slate-500 italic text-center py-12">Jelenleg nincsenek rögzített külső programok.</p>
            )}
          </div>
        </div>
      </section>

      {/* Regisztráció Section */}
      <section id="regisztracio" class="py-20 sm:py-24 bg-slate-900 border-t border-white/5">
        <div class="container mx-auto px-6">
          <div class="grid gap-12 sm:gap-16 lg:grid-cols-2 lg:items-center">
            <div>
              <h2 class="section-kicker">Csatlakozás</h2>
              <h3 class="section-heading">Legyél a közösség tagja</h3>
              <p class="section-copy">
                A MIEF egy nyitott szakmai fórum. Ha érdekelnek az MI etikai kérdései, és szívesen részt vennél a párbeszédben vagy eseményeinken, regisztrálj!
              </p>
              
              <ul class="mt-7 sm:mt-8 space-y-3 sm:space-y-4">
                {[
                  'Értesítések a legújabb eseményekről',
                  'Hozzáférés szakmai anyagokhoz',
                  'Lehetőség workshopokon való részvételre',
                  'Szakmai hálózatépítés'
                ].map((benefit) => (
                  <li class="flex items-center gap-3 text-slate-300">
                    <svg class="h-5 w-5 text-cyan-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                    </svg>
                    {benefit}
                  </li>
                ))}
              </ul>
            </div>

            <div class="rounded-3xl border border-white/10 bg-slate-950/70 p-8 shadow-2xl">
              {submissionStatus === "success" ? (
                <div class="text-center py-12">
                  <div class="mx-auto flex h-16 w-16 items-center justify-center rounded-full bg-cyan-400/20 text-cyan-400">
                    <svg class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                    </svg>
                  </div>
                  <h4 class="mt-6 text-2xl font-bold text-white">Sikeres regisztráció!</h4>
                  <p class="mt-4 text-slate-300">Köszönjük a jelentkezésedet. Hamarosan keresünk!</p>
                  <button 
                    onclick="window.location.hash = ''"
                    class="mt-8 text-sm font-semibold text-cyan-400 hover:text-cyan-300 transition"
                  >
                    Vissza a főoldalra
                  </button>
                </div>
              ) : (
                <form method="POST" class="grid gap-5 sm:gap-6">
                  <div class="grid gap-2">
                    <label for="name" class="text-sm font-medium text-slate-300">Név</label>
                    <input 
                      type="text" 
                      id="name" 
                      name="name"
                      required
                      placeholder="Teljes név" 
                      class="rounded-xl border border-white/10 bg-white/5 px-4 py-3 text-white placeholder:text-slate-500 focus:border-cyan-400 focus:outline-none focus:ring-1 focus:ring-cyan-400 transition"
                    />
                  </div>
                  <div class="grid gap-2">
                    <label for="email" class="text-sm font-medium text-slate-300">E-mail cím</label>
                    <input 
                      type="email" 
                      id="email" 
                      name="email"
                      required
                      placeholder="email@pelda.hu" 
                      class="rounded-xl border border-white/10 bg-white/5 px-4 py-3 text-white placeholder:text-slate-500 focus:border-cyan-400 focus:outline-none focus:ring-1 focus:ring-cyan-400 transition"
                    />
                  </div>
                  <div class="grid gap-2">
                    <label for="profession" class="text-sm font-medium text-slate-300">Szakterület</label>
                    <select 
                      id="profession" 
                      name="profession"
                      required
                      class="rounded-xl border border-white/10 bg-white/5 px-4 py-3 text-white focus:border-cyan-400 focus:outline-none focus:ring-1 focus:ring-cyan-400 transition appearance-none"
                      onchange="document.getElementById('custom-profession-container').style.display = this.value === 'other' ? 'grid' : 'none'; document.getElementById('profession_custom').required = this.value === 'other';"
                    >
                      <option value="" disabled selected class="bg-slate-900">Válassz szakterületet</option>
                      <option value="tech" class="bg-slate-900">Technológia / Mérnök</option>
                      <option value="legal" class="bg-slate-900">Jog</option>
                      <option value="edu" class="bg-slate-900">Oktatás</option>
                      <option value="research" class="bg-slate-900">Kutatás</option>
                      <option value="other" class="bg-slate-900">Egyéb (saját megadás)</option>
                    </select>
                  </div>

                  <div id="custom-profession-container" class="grid gap-2" style="display: none;">
                    <label for="profession_custom" class="text-sm font-medium text-slate-300">Saját szakterület megnevezése</label>
                    <input 
                      type="text" 
                      id="profession_custom" 
                      name="profession_custom"
                      placeholder="Pl. Filozófus, Biztonságpolitika" 
                      class="rounded-xl border border-white/10 bg-white/5 px-4 py-3 text-white placeholder:text-slate-500 focus:border-cyan-400 focus:outline-none focus:ring-1 focus:ring-cyan-400 transition"
                    />
                  </div>

                  {submissionStatus === "error" && (
                    <p class="text-sm text-red-400">Hiba történt a küldés során. Kérjük próbáld újra később!</p>
                  )}

                  <button 
                    type="submit"
                    class="mt-2 sm:mt-4 rounded-xl bg-gradient-to-r from-cyan-400 to-blue-500 py-4 text-sm font-bold text-white shadow-lg shadow-cyan-500/20 transition hover:scale-[1.02] active:scale-[0.98]"
                  >
                    Regisztráció beküldése
                  </button>
                  <p class="text-center text-xs text-slate-400">
                    A regisztrációval elfogadod az adatkezelési tájékoztatónkat.
                  </p>
                </form>
              )}
            </div>
          </div>
        </div>
      </section>

      {/* CTA Section */}
      <section id="kapcsolat" class="py-20 sm:py-24 relative">
        <div class="container mx-auto px-6">
          <div class="relative rounded-3xl overflow-hidden bg-gradient-to-br from-cyan-500 to-blue-600 px-6 sm:px-8 py-14 sm:py-16 text-center shadow-2xl shadow-cyan-500/20">
            <div class="absolute inset-0 bg-[radial-gradient(circle_at_20%_20%,rgba(255,255,255,0.1),transparent)]"></div>
            <h2 class="relative z-10 text-3xl font-bold text-white sm:text-4xl">Kérdésed van?</h2>
            <p class="relative z-10 mt-6 mx-auto max-w-xl text-lg text-cyan-50">
              Szeretnél többet tudni a fórumról, vagy együttműködési javaslatod van? Keress minket bizalommal!
            </p>
            <div class="relative z-10 mt-8 sm:mt-10">
              <a
                href="mailto:info@mief.hu"
                class="rounded-xl bg-white px-8 py-4 text-sm font-bold text-blue-600 transition hover:bg-slate-100 hover:scale-105 active:scale-95 shadow-lg"
              >
                Kapcsolatfelvétel e-mailben
              </a>
            </div>
          </div>
        </div>
      </section>
    </main>

    <Footer />
  </body>
</html>
